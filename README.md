This repo contains various utilities for the SANE environment.

Intended use:

- _cpu.yaml_ for deploying a SANE RV workspace without GPU
- _gpu.yaml_ for deploying a SANE RV workspace with NVidia GPU
- _pw\_install.ps1_ is used for enabling PDF export.
